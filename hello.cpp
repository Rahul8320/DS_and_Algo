#include <bits/stdc++.h>
using namespace std;

void solution(int arr[],int n)
{
    
}

int main()
{
    int size;
    cin >> size;
    int arr[size];
    for(int i=0;i<size;i++)
	{
        cin >> arr[i];
    }
    solution(arr,size);
    return 0;
}
