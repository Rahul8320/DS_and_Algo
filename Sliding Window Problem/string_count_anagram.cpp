// count how many anagram of a pattern present in a given string.

#include<iostream>
#include<map>

using namespace std;

int main()
{
    string str,pattern;
    cin>> str >> pattern;
    int k = pattern.length();
    

    return 0;
}